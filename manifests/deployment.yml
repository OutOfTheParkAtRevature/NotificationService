apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  strategy:
    rollingUpdate:
      maxSurge: 3
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: outoftheparkacr.azurecr.io/notification-service
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              name: notif-http
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service-deployment
  namespace: default
spec:
  ports:
    - name: notif-http
      protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: notification-service
  type: ClusterIP
      